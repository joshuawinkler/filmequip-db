{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "base.schema.json",
  "title": "Equipment entry (base fields, common to every category)",
  "type": "object",
  "required": ["id", "category", "manufacturer", "model"],
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique, immutable ID. Format: manufacturer-model-slug, e.g. arri-alexa-mini-lf",
      "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
    },
    "category": {
      "type": "string",
      "description": "Full breadcrumb path of category names, root to leaf, joined by ' > ', e.g. 'Camera > Cameras > Digital Cameras'. Must resolve to a leaf in data/categories.json.",
      "pattern": "^[^>]+( > [^>]+)*$"
    },
    "manufacturer": { "type": "string" },
    "model": { "type": "string" },
    "notes": { "type": "string" },
    "image_url": {
      "type": "string",
      "format": "uri",
      "description": "Only link to freely licensed images (e.g. Wikimedia Commons), no uploads of your own into the repo"
    },
    "source_url": {
      "type": "string",
      "format": "uri",
      "description": "Link to manufacturer page/datasheet for verification"
    }
  }
}
